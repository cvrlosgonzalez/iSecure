<%= form_for(@cam) do |f| %>
  <% if @cam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cam.errors.count, "error") %> prohibited this cam from being saved:</h2>

      <ul>
      <% @cam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :alert %><br>
    <%= f.text_field :alert %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
